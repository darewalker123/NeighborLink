export type Role = 'USER' | 'SERVICE_PROVIDER' | 'ADMIN';
export type BookingStatus = 'PENDING'|'ACCEPTED'|'REJECTED'|'CANCELLED'|'IN_PROGRESS'|'COMPLETED'|'DISPUTED'|'REFUNDED';
export type User = { id:string; fullName:string; email:string; phone?:string; role:Role; neighborhood?:string; city?:string; avatarUrl?:string; status?:string; providerProfile?: {id:string;verificationStatus:string;averageRating:number} };
export type Service = { id:string; title:string; description:string; price:number|string; durationMin:number; category:{id:string;name:string;slug:string;icon?:string} };
export type Provider = { id:string; bio?:string; skills:string[]; experienceYears:number; serviceRadiusKm:number; averageRating:number; reviewCount:number; completedJobs:number; verificationStatus:string; portfolioUrls:string[]; isAcceptingWork:boolean; user:Omit<User,'role'|'email'>; services:Service[]; availability:{id:string;dayOfWeek:number;startTime:string;endTime:string;unavailable:boolean}[]; startingPrice:number|null; distance:number|null; recommendationScore:number; reviewsReceived?:Review[] };
export type Review = { id:string; rating:number; comment:string; createdAt:string; author:{fullName:string;avatarUrl?:string} };
export type Booking = { id:string; scheduledStart:string; scheduledEnd:string; quotedPrice:number|string; status:BookingStatus; notes?:string; locationNote?:string; createdAt:string; updatedAt:string; completedAt?:string; service:Service; customer:{id:string;fullName:string;avatarUrl?:string;phone?:string;neighborhood?:string}; provider:{id:string;user:{id:string;fullName:string;avatarUrl?:string;phone?:string;neighborhood?:string}}; payment?:{status:string;amount:number|string;paidAt?:string}; review?:Review; conversation?:{id:string} };
export type Page<T> = {items:T[];pagination:{page:number;limit:number;total:number;pages:number}};
